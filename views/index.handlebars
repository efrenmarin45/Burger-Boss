<h1 class="title d-flex justify-content-center">Burger Boss</h1>

<h2>Devoured Orders</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Ready to Eat!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Order Here:</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Place Order:</label>
    <input type="text" id="ca" name="name" placeholder="Big Mac, Fatburger, Whopper" >
  </div>

  <div class="form-group">
    <label for="devoured">Confirm Order</label>
    <input class="larger" type="checkbox" name="devoured" value="1">
  </div>

  <button type="submit">Submit Order</button>
</form>